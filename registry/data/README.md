# Local registry data

Store raw source downloads, licensed releases, caches, and generated outputs here.

The directory contents are ignored by Git except for this README and `.gitignore`.

Do not store credentials or secrets here.
